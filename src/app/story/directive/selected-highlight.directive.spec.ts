import { SelectedHighlightDirective } from './selected-highlight.directive';

describe('SelectedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
